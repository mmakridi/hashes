#include <hashes.hpp>

// template<typename K, typename V>
// HashMap_Chained<K, V>::HashMap_Chained(size_t n) {
//     this->data.reserve(n);
// }

// template<typename K, typename V>
// void HashMap_Chained<K, V>::insert() {

// }
